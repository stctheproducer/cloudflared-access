#!/bin/bash

set -e

echo "=> Post-build hook running"
# Example: cleanup or notifications
docker buildx rm custom-builder || true

# Example: tag management
if [ "$SOURCE_BRANCH" = "main" ]; then
    docker tag $IMAGE_NAME $DOCKER_REPO:latest
fi